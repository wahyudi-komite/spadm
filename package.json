{
  "name": "spadm-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for managing backend and frontend concurrently",
  "scripts": {
    "start:dev": "npm --prefix backend run start:dev",
    "start": "npm --prefix frontend/starter run start",
    "dev": "npx concurrently -n \"BE,FE\" -c \"yellow,cyan\" \"npm run start:dev\" \"npm run start -- --open\""
  }
}
